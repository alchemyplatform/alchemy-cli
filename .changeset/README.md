# Changesets

This folder is managed by [Changesets](https://github.com/changesets/changesets).

Changeset files in this directory describe pending version bumps and release notes. They are consumed by `changeset version` and removed after the version is applied.

To add a changeset:

```bash
pnpm changeset
```
