---
"@alchemy/cli": minor
---

Address arguments now accept ENS names (e.g. `alchemy balance vitalik.eth`). Supported on `balance`, `transfers`, `tokens`, and `nfts` commands when targeting Ethereum networks.
