---
"@alchemy/cli": minor
---

Moved `chains list` to `apps chains` for clearer separation from `network list`. Use `apps chains` to list Admin API chain identifiers (e.g. ETH_MAINNET) and `network list` for RPC network IDs (e.g. eth-mainnet).
