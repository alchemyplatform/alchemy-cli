---
"@alchemy/cli": patch
---

Adopt a changesets-based release workflow so user-facing CLI updates are versioned and documented consistently. This also introduces an automated publish pipeline that creates releases and npm publishes from validated changesets.
